{
    "environment": {
        "logging": {
            "console": true,
            "console_level": "INFO",
            "file_level": "INFO"
        },
        "collisions": false,
        "time_limit": 1000,
        "num_runs": 1,
        "ticks_per_second": 20,
        "_comment_ticks_per_second": "ARENA clock. Separate from the agent's ticks_per_second and defaulting to 3 if absent. The agent's value sets the DDM's dt; this one drives the simulation loop and the logging cadence. If they disagree the arena samples the agent only a few times per second while it integrates far faster, and the tick stamps alias against the snapshot offsets - a 5.5 s decision then renders as one or two points and looks instantaneous. Keep them equal.",
        "arenas": {
            "arena_0": {
                "random_seed": 0,
                "radius": 1,
                "_id": "square",
                "color": "gray"
            }
        },
        "termination": {
            "type": "proximity",
            "target_ids": [
                "static_0.s#0",
                "static_1.s#0"
            ],
            "radius": 0.05,
            "agent_ids": "any"
        },
        "objects": {
            "static_0": {
                "number": [
                    1
                ],
                "position": [
                    [
                        0.433,
                        -0.25,
                        0
                    ]
                ],
                "_id": "idle",
                "shape": "cylinder",
                "height": 0.2,
                "diameter": 0.05,
                "color": "red",
                "strength": [
                    4.0
                ],
                "uncertainty": [
                    0
                ]
            },
            "static_1": {
                "number": [
                    1
                ],
                "position": [
                    [
                        0.433,
                        0.25,
                        0
                    ]
                ],
                "_id": "idle",
                "shape": "cylinder",
                "height": 0.2,
                "diameter": 0.05,
                "color": "green",
                "strength": [
                    5.0
                ],
                "uncertainty": [
                    0
                ]
            }
        },
        "agents": {
            "movable_0": {
                "position": [
                    [
                        0.0,
                        0.0,
                        0.0
                    ]
                ],
                "orientation": [
                    [
                        0,
                        0,
                        0
                    ]
                ],
                "number": [
                    1
                ],
                "shape": "cylinder",
                "linear_velocity": 0.05,
                "angular_velocity": 10,
                "ticks_per_second": 20,
                "height": 0.02,
                "diameter": 0.033,
                "color": "blue",
                "detection": {
                    "type": "GPS",
                    "range": 5.0,
                    "acquisition_per_second": "inf"
                },
                "moving_behavior": "embodied_pure_ddm",
                "_comment_timing": "One integration step per control tick, across ticks, in per-second units (with n_sub sub-steps). BY DESIGN \u2014 see EMBODIED_DDM_PLAN.md Section 0.3. Do not add internal relaxation steps.",
                "embodied_pure_ddm": {
                    "_comment_structure": "--- structure. K == 2 ONLY: the pure DDM is the SPRT for a binary hypothesis test, and there is no scalar log-likelihood ratio for K > 2. For K > 2 use moving_behavior 'embodied_ddm' with accumulator_mode 'race' (the MSPRT). Exactly 2 target_ids are required; their order fixes the sign of x (+z favours the first). ---",
                    "target_ids": [
                        "static_0.s#0",
                        "static_1.s#0"
                    ],
                    "guard_ids": [],
                    "reference": "egocentric",
                    "perception_width": 0.2,
                    "perception_global_inhibition": 0.2,
                    "num_neurons": 30,
                    "_comment_num_neurons": "Only sizes the SHARED detection ring so perception is bit-identical to the ring attractor. The DDM never reads it to decide.",
                    "task": "selection",
                    "sensory_time_mode": "world_time",
                    "_comment_evidence": "--- evidence. eta_rate is a noise RATE: eta_i = eta_rate_i/sqrt(dt), so c^2 = sum of squares and results are tick-rate independent (Section 1.3). ---",
                    "evidence_mode": "difference",
                    "_comment_evidence_mode": "difference (default; the SPRT when eta_rate entries are equal) | llr (precision-weighted; needs the deferred per-target uncertainty field and currently raises).",
                    "eta_rate": [
                        0.25,
                        0.25
                    ],
                    "dist_mode": "none",
                    "_comment_dist_mode": "Shared with embodied_ddm and applied to q BEFORE sampling. none (default, matches the ring attractor) | exp | subtense | loom. Any non-none setting makes A time-varying and invalidates the closed-form ER/DT of Section 3.1.",
                    "attention_mode": "none",
                    "_comment_attention_mode": "none (default) | vonmises | saccade. Shared with embodied_ddm.",
                    "target_quality_modulations": {},
                    "extended": false,
                    "_comment_extended": "Across-trial variability ([B06] Section 3): s_A > 0 gives slow errors, s_x > 0 gives fast errors. NOTE s_A > 0 is one of the assumptions whose violation JUSTIFIES collapsing bounds (Section 4.2), so it is theoretically linked to boundary_mode, not independent of it.",
                    "x0": 0.0,
                    "_comment_integration": "--- integration ---",
                    "n_sub": 5,
                    "_comment_boundary": "--- boundary. WARNING: threshold_policy bayes_risk / reward_rate give the optimal CONSTANT boundary. They do NOT justify a collapsing boundary \u2014 under their own assumptions the constant boundary is provably optimal (Section 4.1, Appendix C). Combining them with boundary_mode 'collapsing' logs a warning and stamps boundary_policy_incoherent into the record; never report that combination as optimal. ---",
                    "boundary_mode": "static",
                    "collapse_form": "geometric",
                    "_comment_collapse_form": "Only 'geometric' is meaningful here: it re-evaluates rho from the live angular separation each tick rather than applying a parametric decay. Ignored entirely when boundary_mode is 'static'. The descriptive forms (weibull/hyperbolic/exponential/linear) need their own shape parameters, which this template omits.",
                    "_comment_boundary_mode": "static | collapsing",
                    "threshold_policy": "geometric",
                    "_comment_threshold_policy": "manual | bayes_risk | reward_rate | geometric. Coherent with collapsing: manual (descriptive) and geometric (derived) only.",
                    "cost_ratio": 1.0,
                    "_comment_cost_ratio": "c_e/c_tau. ONE parameter: only the ratio affects argmin BR (Section 3.3). Ignored under the geometric policy, which derives both costs from geometry.",
                    "T0": 0.0,
                    "_comment_T0": "Non-decision time = MEASURED actuator lag, not a fitted nuisance parameter. The decision latches internally on crossing and reaches the motors T0 seconds later.",
                    "threshold_update_ticks": 0,
                    "_comment_threshold_update_ticks": "0 = compute z* once at evidence onset; >0 = recompute every N ticks. Under geometric, z* is state-dependent (d and Delta change as the agent moves) so >0 is the meaningful setting \u2014 but do not combine a recomputed z* with an explicit collapse_form without logging both.",
                    "geometric_error_mode": "terminal_categorical",
                    "_comment_geometric_error_mode": "terminal_graded (c_e = A/lambda_t; rho = (A/c)^2 * c_e / c_tau(Delta), so rho ~ A^3) | terminal_categorical (options differ in kind, 0-1 loss: c_e = cost_ratio, rho = (A/c)^2 * cost_ratio / c_tau(Delta) with c_tau = 2 sin^2(Delta/4)). The error mode sets c_e ONLY; c_tau is shared with the other modes and carries ALL the angular dependence, hence the entire collapse. | correctable (c_e = 2 d sin(Delta/2)/v; rho ~ A^2 and DOES produce an arrival deadline).",
                    "delta_min": 0.001,
                    "_comment_delta_min": "Clamp on the angular separation: sin^2(Delta/4) -> 0 sends rho -> inf (targets in the same direction means deliberation is free). Keeps sinh() from overflowing.",
                    "A_source": "online_lognormal",
                    "_comment_drift_knowledge": "TASK_A_KNOWN_DRIFT.md: the implemented model assumes the agent knows |A| but not its sign, and permits only A_source 'ensemble' (A_expected, fixed for the block) or 'oracle' (the true per-trial |q0-q1|). This config uses 'online_lognormal', which INFERS |A| from the evidence -- an estimation model that belongs to TASK_B_UNKNOWN_DRIFT.md. It is declared here so the config keeps running unchanged; under it `a` is NOT a confidence and the collapse has a second source besides the geometry. To make this the clean known-|A| baseline instead, set A_source 'oracle' (identical to online_lognormal with A_lognormal_s = 0) and drop this key.",
                    "drift_knowledge": "estimated",
                    "_comment_A_source": "Where the A entering rho AND z* comes from (it enters twice: rho ~ A^2*c_e/(c^2*c_tau) and z* = a*c^2/(2A)). Under the KNOWN-|A| model (TASK_A_KNOWN_DRIFT.md) only two options are coherent: 'ensemble' = A_expected, fixed for the whole block -- the agent knows the magnitude characteristic of its environment. A_expected is REQUIRED; there is no null -> running-mean fallback, which was undefined at evidence onset and made z* depend on trial history. 'oracle' = the true per-trial |q0-q1|; equivalent to ensemble in a fixed scenario and differing only if target qualities vary across trials, in which case it is an oracle assumption (calibration and V-tests only). 'online_evidence' (|x_t|/t) and 'online_lognormal' (|q0-q1|*exp(eps), eps ~ N(0,s^2), drawn once at onset) are ESTIMATION models: they infer |A| from the evidence and belong to TASK_B_UNKNOWN_DRIFT.md. They are REJECTED here unless drift_knowledge is set to 'estimated'. Under them `a` is no longer the log-odds at commitment and the collapse acquires a second source besides the geometry.",
                    "evidence_target": "quality",
                    "_comment_evidence_target": "quality (default) | net_value. Strictly, under an asymmetric layout the agent should accumulate net value q_i - lambda_t*d_i/v, since a nearer but slightly worse target can be the correct choice; net_value raises NotImplementedError for now and is irrelevant in the symmetric case.",
                    "z_min": 0.05,
                    "urgency_mode": "off",
                    "_comment_urgency_mode": "off | boundary | gating. 'gating' low-pass filters the evidence and MULTIPLIES by urgency rather than integrating (Cisek et al.) \u2014 a genuinely different mechanism, NOT a reparameterisation of a collapsing bound.",
                    "_comment_readout": "--- readout / motion ---",
                    "readout_mode": "normalized",
                    "_comment_readout_mode": "normalized (default; P1 = clip((1+x/z(t))/2,0,1), needs no drift estimate and sharpens automatically as a collapsing boundary closes) | posterior (P1 = sigmoid(2*A_hat*x/c^2); principled but presumes the agent knows the drift).",
                    "predecision_motion": "midpoint",
                    "_comment_predecision_motion": "average (default; belief-weighted heading \u2014 a gradual turn toward the leader as x accumulates; the setting comparable to the ring attractor's pre-bifurcation spatial averaging) | midpoint (FIXED angular bisector of the two BEARINGS while deliberating, independent of x, then a STEP to the chosen bearing on commitment; beliefs are still computed and logged, only the heading ignores them) | forward (belief-weighted heading, constant speed) | stationary (the NON-EMBODIED control: no translation and no rotation, so bearings never change and there is no feedback loop). NOTE: coupled to the geometric c_tau derivation \u2014 under stationary c_tau == 1 identically, and under midpoint c_tau = 1 - cos(Delta/2) is EXACT rather than a near-tie approximation.",
                    "_comment_embodiment_arms": "The embodiment loop has two arms: decision->motion (x influences the heading) and motion->threshold (pose changes Delta, which changes z* via rho(Delta)). stationary cuts both; midpoint cuts only the first; average keeps both. An effect present in average but not midpoint belongs to the first arm; one present in midpoint but not stationary belongs to the second.",
                    "scaling_mode": "constant",
                    "_comment_scaling_mode": "concentration | constant | magnitude. Under midpoint, concentration == cos(Delta/2) and is PURELY GEOMETRIC: it carries no decision information and drives speed to zero as Delta -> pi, so the agent creeps to the midpoint and hovers until noise resolves the tie (the closest behavioural match to the ring attractor at a symmetric tie). Use constant to disable gating so midpoint does exactly one thing; use concentration deliberately if the hovering is the object of study.",
                    "norm_scale": 1.0,
                    "_comment_norm_scale": "Scaling used when scaling_mode is 'constant'.",
                    "bisector_eps": 1e-06,
                    "_comment_bisector_eps": "Hold the previous heading when |sum of unit bearing vectors|/2 falls below this (Delta -> pi, where the vectors cancel and atan2(0,0) is undefined). A guard that fires repeatedly means the agent is parked between the targets \u2014 a result, not an error; it is logged per tick as bisector_guard_fired.",
                    "allow_changes_of_mind": false,
                    "A_expected": 1.0,
                    "bifurcation": {
                        "mode": "behavioral",
                        "_comment_mode": "behavioral for ALL three models so switch counts and alignment events are measured by identical code.",
                        "lambda_threshold": -0.1,
                        "spike_min_separation": 10,
                        "alignment_tolerance_deg": 5.0,
                        "alignment_consecutive_ticks": 5,
                        "gradient_window": 5,
                        "gradient_threshold": 0.005
                    },
                    "_comment_calibration": "eta_rate 0.25 (c = 0.354) with cost_ratio 1.0, for the agent starting at (0,0) so Delta(onset) = 60 deg. Chosen so the boundary clears the per-tick noise across most of the quality-difference grid: at ticks_per_second 20 one tick of noise is c*sqrt(dt) = 0.079, against z* peaking at 0.405 near delta ~ 7%. Degenerate points (noise > half the boundary) drop from 10/22 at eta_rate 1.0 to 6/22. NOTE z* is NON-MONOTONIC in delta under terminal_categorical: it rises to ~0.405 at delta ~ 7% and falls again, so the residual degenerate points are now at the EASY end (delta >= 64%), where accuracy is ~1.0 anyway and only DT is uninformative. Raising cost_ratio is the only lever that lengthens decisions (z* ~ cost_ratio in the small-rho branch); lowering eta_rate reduces noise without moving z* there.",
                    "_comment_scope": "TEMPLATE for the geometric threshold policy under BOTH boundary regimes. Switch with two keys only: STATIC  -> boundary_mode: 'static',     threshold_update_ticks: 0  (z* derived once from the onset geometry, then held). COLLAPSING -> boundary_mode: 'collapsing', threshold_update_ticks: 1 (z* re-derived from the live Delta each tick; collapse_form 'geometric' IS that re-evaluation). Both start from the same z*(onset), so they differ in exactly one thing: whether the boundary may follow the geometry. Trimmed to the parameters read under geometric_error_mode: terminal_categorical, dist_mode: none, attention_mode: none, extended: false, readout_mode: normalized, urgency_mode: off, allow_changes_of_mind: false; anything omitted falls back to its code default. See config/embodied-pure-ddm-2-targets.json for the fully documented parameter set.",
                    "A_lognormal_s": 0.1,
                    "_comment_A_lognormal_s": "Multiplicative noise scale on the drift-magnitude percept; ~ coefficient of variation. 0.0 reproduces oracle EXACTLY. Only read under online_lognormal.",
                    "A_lognormal_debias": true,
                    "_comment_A_lognormal_debias": "rho ~ A_hat^2 is convex, so noise inflates rho by exp(2 s^2) in expectation - the agent would get more cautious purely as an artefact of the square. true (default): A_hat = |A|*exp(eps - s^2), giving E[A_hat^2] = A^2, so noise adds variability without shifting the mean threshold (the clean comparison to s=0). false: uncertainty RAISES the threshold - defensible, but state it as a claim.",
                    "A_lognormal_redraw": "onset",
                    "_comment_A_lognormal_redraw": "onset only. A per-tick redraw would turn a percept into a noise process and interact opaquely with threshold_update_ticks.",
                    "A_hat_min": 1e-09,
                    "_comment_A_hat_min": "Division guard for z* = a*c^2/(2 A_hat); not a behavioural floor."
                }
            }
        },
        "results": {
            "base_path": "../data/",
            "agent_specs": [
                "base",
                "mean_field"
            ],
            "group_specs": [],
            "snapshots_per_second": 20
        }
    }
}
